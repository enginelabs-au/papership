# Outstanding tasks

Canonical live list. The lead agent updates this file every substantive turn and repeats the **open** rows at the end of the reply, even when the owner did not mention them.

Do not store secret values here. Charges stay off until a later owner flip.

| ID | Status | Owner | Task |
|---|---|---|---|
| OT-01 | done | — | Leftover feature branches are gone from git. This tree has only `main` @ `63a3e3a` and `origin/main`. GitHub has no leftover branches; PRs 1–5 are closed. If Cursor still lists the old names, that is a stale picker / closed-PR label, not a live branch. |
| OT-02 | done | — | OQ-G2 recorded on local API store 2026-09-12T15:20Z (`oq_g2_recorded: true` at `/tmp/papership-api-store.sqlite`). Vercel still cannot hold this flag. |
| OT-03 | done | — | Public Gmail/Slack client IDs and Gmail redirect are set in `~/.config/papership/connectors.env`. Hermes env was not copied. |
| OT-04 | done | — | Gmail/Slack OAuth start, callback, and sealed token exchange shipped. Tests: `services/api/tests/test_oauth.py` 7 passed. Live connect waits on OT-20 secrets. |
| OT-05 | done | — | Hermes user systemd has no `Environment=EMAIL_*`. Live `hermes-gateway.service` only has PATH / VIRTUAL_ENV / HERMES_HOME / HERMES_SUPERVISED_CHILD. Serve already uses `EnvironmentFile`. Values were not printed. |
| OT-06 | later | — | Parked. Off-VPS backup + restore drill is not current work. Logged in `docs/handover/future-tasks.md`. |
| OT-07 | later | — | Parked. Optional GitHub branch protection / Dependabot / pin Actions by SHA. Owner said ignore. |
| OT-08 | later | — | Parked. Flip `ENGINE_BILLING_CHARGES_ENABLED` later. Remind on request. |
| OT-09 | later | — | Parked. Apple / Google store accounts and submit. Remind on request. |
| OT-10 | later | — | Parked. Two Security passes (2026-09-12 and 2026-09-13) are CONDITIONAL. Write/external Hermes `accepted` stays unauthorized. Five architectural gaps remain. See `docs/workstreams/20260913-d25-repass/security-engineer-subagent/handoff.md`. |
| OT-11 | later | — | Parked. Hermes WhatsApp/Telegram chat is already live. Papership Integrations rows stay planned unless you later want Inbox/send-with-approval inside Papership. Do not copy Hermes env. |
| OT-12 | later | — | Parked. Signed desktop / store binaries. Same family as OT-09. Unsigned Tauri/PWA already run. |
| OT-13 | later | — | Parked. Org-wide erase / Droplet destroy is not recommended and is not tracked as live work. |
| OT-14 | later | — | Parked. Today decorative fixture rows are leftover blueprint chrome. Insignificant. |
| OT-15 | done | — | Founder JWT no longer persists in `localStorage`. Session is in-memory + `sessionStorage`; leftover `localStorage` keys are cleared; sign-out wipes the token. Residual: XSS can still read `sessionStorage`. httpOnly cookies need same-origin (Vite is `localhost:5173` → API `127.0.0.1:8000`). |
| OT-16 | done | — | Live Hermes `GET /health` on `127.0.0.1:8642` returned 200 in ~1ms (HEAD 405). Gateway and serve were active. Papership probe stays HEAD-first. |
| OT-17 | done | — | Trial rate card Free / Pro / Max / Enterprise published in Settings → Plan. Charges off. |
| OT-18 | done | — | Desktop Tauri loads current `/papership` web chrome. |
| OT-19 | done | — | `main` pushed as Cursor Agent; production `/papership` READY (`63a3e3a`). |
| OT-20 | done | — | `connectors.env` has Gmail and Slack client IDs, secrets, and redirect URLs. Live `/health` `oauth.gmail` and `oauth.slack` are `ready`. Values were not printed. |
| OT-21 | done | — | Local `/papership` Continue accepts blank or any email/password. There is no real password. Use `http://localhost:5173/papership`, not Vercel, for API-backed work. |
| OT-22 | done | — | Local API CORS now allows `http://localhost:5173` as well as `http://127.0.0.1:5173`. Connect fetch from Vite localhost no longer looks like a dead API. |
| OT-23 | done | — | Local API calls mint `/auth/local-session` when `papership-token` is missing, so Integrations connect no longer fails with missing bearer after a chrome-only sign-in. |
| OT-24 | done | — | Local API no longer segfaults (exit 139) on the overlay burst. SQLite access is serialized; uvicorn uses `--loop asyncio --http h11`. Concurrent `/people` `/teams` `/inbox` `/connections` `/memory` `/strategy` now stay 200. |
| OT-25 | done | — | Local Gmail OAuth completed. `/connections` shows `gmail` `configured`, `enabled`, `has_token`. Send stays approval-then-receipt. Token values were not printed. |
| OT-26 | done | — | Local Slack OAuth completed. `/connections` shows `slack` `configured`, `enabled`, `has_token` after `GET /oauth/slack/callback` 302 at 16:30Z. Send stays approval-then-receipt. Token values were not printed. |
| OT-27 | done | — | Integrations Set up now preselects that row’s provider. Slack Set up no longer starts Gmail. OAuth return opens the Integrations tab. |
| OT-28 | done | — | Local founder JWT is reminted when expired or within 90s of expiry. A 401 retries once so Slack Set up no longer dies with `invalid token: Signature has expired`. |
| OT-29 | done | — | Hermes desktop/droplet inference pinned to OpenRouter. Stale Nous `active_provider` and an exhausted OpenRouter pool flag were cleared. Free OpenRouter fallbacks (`poolside/laguna-xs-2.1:free`, `thinkingmachines/inkling:free`) stay. |
| OT-30 | done | — | Hermes status chip “Gateway · inference unavailable” was a false negative: `setup.runtime_check` saw OpenRouter configured but the serve process had no router key. Key is now in the serve EnvironmentFile; serve restarted; runtime resolve is usable. |
| OT-31 | done | — | OpenRouter Settings picker now lists the live tool-capable catalog (377). Cap is 999 / uncapped, not None. |
| OT-32 | done | — | Hermes Desktop chat model selector no longer hangs on the live 377-id OpenRouter catalog. Featured is the curated shortlist (~48) plus the current pick (`openai/gpt-5.6-luna-pro` in featured). Capability lookups skip the long tail. Serve restarted; reconnect Desktop once. |
| OT-33 | done | — | Hermes Desktop model pickers filter as you type. Chat catalog focuses its search field on open. Settings / fallback / aux / MoA / cron / bots Selects use `searchable`. |
| OT-34 | done | — | Local Papership desktop rebuilt from current `@papership/web` and opened. Bundle is `Papership.app` (was leftover `OrgOS.app`). Window title and display name are Papership. Workspace package is `@papership/desktop`. |
| OT-36 | done | — | Papership desktop/OS icons are full-bleed opaque squares so macOS/iOS can apply the system squircle. Pre-cut plate stays on web/tab/`brand/papership-icon.png` only. New `icon.icns` copied into the local `Papership.app` and the app reopened. |
| OT-35 | done | — | Hermes picker fixes opened as NousResearch PR https://github.com/NousResearch/hermes-agent/pull/109304. Local `Hermes.app` rebuilt from `07de97ddce` and installed. Unsigned because two identical Apple Development identities collide. |
| OT-37 | done | — | Hermes Settings type-to-search no longer steals model-picker keystrokes. Printable keys go to the picker filter even when Radix left focus on a row. Pushed `9fd6983c20` to `cam-douglas/hermes-agent` (`fix/openrouter-picker-and-search`); PR https://github.com/NousResearch/hermes-agent/pull/109304 updated. Local `Hermes.app` rebuilt unsigned and installed. |
| OT-38 | done | — | Hermes Desktop can live-edit chrome without a rebuild: `~/.hermes/desktop-chrome/overlay.css` (blocked constructs rejected; last-known-good in `.good/`), ⌘K reload/rollback, `chrome.slots` + `model-pill` falls back to the bundled pill if a plugin throws. Composer/settings stay kernel. Tests 38 passed. Unsigned `Hermes.app` installed. Not committed. |
| OT-39 | done | — | `claude-design` v1.2.0 remaps `show_html()` to `::preview{file="…"}` on Desktop. Skill installed in local `~/.hermes/skills` and both droplet copies. `tests/skills/test_claude_design_skill.py` passed. New Desktop chat (or a skill reload) picks it up — no app rebuild. |
| OT-40 | done | — | Droplet was not down. Desktop lost `root@hermes-droplet-campbell` SSH mux while load sat ~66 on 2 cores; gateway watchdog exit 75; root isolated serve rewrote `auth.json` as root. Tailscale + `hermes serve :9119` stayed up. Restored `hermes` ownership on auth/checkpoints/nous_auth. Reconnect Desktop as `hermes`, not root. |
| OT-41 | done | — | Killed leftover root isolated `hermes serve`. Desktop `connections.json` / `connection.json` now `user: hermes`. SSH config: `hermes-droplet-campbell` + `hermes-vps` = hermes; `droplet-campbell` stays root. Reopened Hermes; log shows `hermes@hermes-droplet-campbell`; isolated serve is hermes-owned under `/home/hermes/.hermes/desktop-ssh/`; `auth.json` still hermes. Backend ready. |
| OT-42 | done | — | New Mac terminals start at `/Users/camdouglas` instead of the papership workspace. Cursor user `terminal.integrated.cwd` + `splitCwd: initial`. Terminal.app new window/tab working-directory behavior set to home (0). Existing open tabs keep their cwd until closed. |
| OT-43 | done | — | cmux does not read Terminal.app or Cursor cwd. It restores its own last workspace (`~/papership`) and new workspaces inherit that by default. Set `app.workspaceInheritWorkingDirectory: false` in `~/.config/cmux/cmux.json`, Ghostty `working-directory = home`, and the saved session to `/Users/camdouglas`. Relaunched cmux; session stayed on home. |
| OT-44 | done | — | Desktop “could not safely reserve this session” was fail-closed lease claim: `/home/hermes/.hermes/runtime/active_sessions.json` was `root:hermes` mode 600 after the earlier root SSH isolated serve. Isolated serve is hermes and could not read it. Chowned to hermes. App and remote backend were already up. Retry the turn; stay on `hermes@`. |
| OT-45 | done | — | All four Hermes turn/tool caps set to 100 on Mac `~/.hermes/config.yaml` and droplet `/home/hermes/.hermes/config.yaml` (as hermes): `agent.max_turns`, `code_execution.max_tool_calls`, `goals.max_turns`, `delegation.max_iterations` (droplet delegation was 250). Infinite not set. Existing Desktop chats keep the old baked-in cap until a new chat. |
| OT-46 | done | — | Desktop 100-step hits were real (`openai/gpt-5.6-luna`, `api_calls=100/100`). Wrap-up then 400ed because session `reasoning_config.effort` is `ultra` and the iteration-summary path leaked it to OpenRouter. Clamped summary extra_body to the wire vocab (`ultra`→`max`) in local + droplet `chat_completion_helpers.py`. Isolated serve restarted so Desktop must reconnect. Tests: `tests/agent/test_reasoning_effort_wire_translation.py` 5 passed. |
| OT-47 | done | — | Owner asked why Luna burns tokens on repeated dumps. Cause: compaction re-injects the todo list, model re-runs the same ~39k `hermes_tools` inspect, context fills, compress, repeat. Enabled `tool_loop_guardrails.hard_stop_enabled` on Mac + droplet. New chat still required; this thread is the loop. |
| OT-48 | done | — | Droplet outbound SMTP 25/465/587 to Gmail times out at DigitalOcean (UFW already allows 465/587). IMAP 993 and HTTPS 443 are open. Left `platforms.email.enabled: false` so the legacy adapter does not sit in SMTP timeouts. Added localhost `127.0.0.1:2525` Gmail-API relay (`gmail-smtp-relay.service`, EHLO 250) and patched droplet `_standalone_send` to send via Gmail API. Isolated serve restarted. Did not send a test message. DO still blocks real SMTP; only a support ticket unblocks 587 to the internet. |
| OT-49 | done | — | Owner re-enabled Hermes Email after OT-48. Gateway log 15:35–16:00Z: IMAP ok, then `[Email] SMTP connection failed: timed out` (~90s) on every connect; reconnect loop; shutdown watchdog exit 75 (event loop missed 3 liveness probes). Desktop also issued several “Stopping gateway for restart”. Yaml is `platforms.email.enabled: false` again; gateway stable since 16:03Z. Keep Email disabled. Inbound = Gmail API poller. Outbound = Gmail API / localhost :2525. Enabling Email still probes real SMTP (env host wins over yaml 127.0.0.1) and will bounce the gateway. |
| OT-50 | done | — | Cursor Ink conversation (not Hermes chat, not xterm scrollback) had no wheel handler. Patched VPS `2026.09.10-fd3934a/1931.index.js` so SGR wheel calls the same `U()` offset path as Up/Down (small → ≤3 lines, flick ≥5 → page). Ignores when `inputDisabled`. Does not inject keys, does not touch xterm `scrollTop`, does not touch `vps-ui-sync`. Mapping test + `node --check` passed. `~/bin/agent` re-applies the patch after a cursor-agent update. Preserve: `scripts/patch-cursor-agent-wheel.py`. Owner closed. |
| OT-51 | done | — | Droplet Cursor CLI already had `approvalMode: unrestricted` and sandbox disabled; new TUI sessions still asked for Run Everything. Installed `/home/hermes/bin/agent` wrapper that execs `cursor-agent --yolo --sandbox disabled --approve-mcps` (skips login/update/status/worker). Login PATH now prefers `~/bin`. Existing live `agent` pane keeps its old mode until you start a new one. |
| OT-52 | done | — | Cursor-finish chime: Desktop plays Glass.aiff + a “Cursor finished” toast on BEL/OSC 9. VPS finish script rings the SSH tty only. Tests 15 passed. Unsigned `Hermes.app` installed. Owner closed. |
| OT-53 | done | — | Persist Hermes Desktop terminal panes across quit via named tmux (`h-<tabId>`). Reopen remounts tabs and `tmux attach`, or `agent --resume` / `--continue`. VPS helpers in `~/bin`. Tests 96 passed. Unsigned app installed. Owner closed. |
| OT-54 | done | — | Gateway dropouts are compute starvation, not a flaky gateway. Droplet is **2 vCPU / 3.8 GiB / 0 swap**. Live: load **54–58**, RAM **3.7/3.8 GiB** used (**83–145 MiB available**). Three Cursor CLI agents (~1.5 GiB), isolated + public serve, gateway, WhatsApp Chrome, Hermes Chromium, Postgres. Kanban: `memory pressure is critical`. Slack/Telegram/WhatsApp sockets drop; Desktop liveness then SSH timeouts (21 stale-drops, 88 SSH timeouts in `desktop.log`; latest 19:20Z). Email still off (OT-49). **Need 8 GB floor / 16 GB if two Cursor panes stay.** DO Basic 8/16 is $48/$96. Cheaper droplet-like: Hetzner CX33/CX43 ~$10/$18.50 (hourly + cap; delete to stop). Contabo ~$6.40 / $16 for 8/24 GB (flat month). Sydney hourly: BinaryLane A$39 / A$78. Canvas: `vps-droplet-price-compare`. Until upgrade, run one Cursor pane. |
| OT-55 | done | — | Owner asked to stop rogue drain so one Cursor pane can finish, and park other Hermes work so it wakes on interact. Immediate: kept live `cursor-agent` 1179536; killed two extras (including a `--resume` and a SIGSTOP’d persist pane that still held 616 MiB); killed a leftover `hermes sessions export` of the old Luna loop chat; stopped root WhatsApp `wwa` + Chrome. Load **50 → 6**, available RAM **~0.8 → 1.8 GiB**. Gateway + `hermes-serve` + Gmail watcher stay up (needed for Desktop; cheap vs Cursor). Extra Cursor cannot RAM-sleep — SIGSTOP still occupies RSS — so extras are **exited** while one pane is interactive (`~/bin/hermes-term-sleep-wake` cron). Wake = attach the tmux tab and `agent --resume` / new `agent`. WhatsApp Chrome stays down until that platform is used again. Upgrade still required (OT-54). |
| OT-56 | done | — | Messaging idle is deterministic, not a `hermes-msg` wake and not chat “sleep”/“wake”. `hermes-gateway` + Gmail watcher **stay up** so Slack/Telegram/iMessage/Gmail work on the phone without a command. Chrome park is OT-57. Email platform still `enabled: false`. |
| OT-57 | done | — | Tighter Chrome park for VPS load. `chromium-cdp` disabled from login (on-demand only; MemoryMax 256M / CPUQuota 20% already set). Timer every **1 min**: gateway stays up; leftover wwa/puppeteer always killed; official CDP Chrome stopped unless Desktop is up **and** it used ≥1% CPU or is younger than 120s. SIGSTOP was not used — it keeps RSS. Verified: `chromium_cdp=down`, `chrome_parked=yes`, no chrome procs, gateway+gmail active, load 0.12, ~1.2 GiB available. |
| OT-58 | done | — | Spend-tracker Desktop plugin on the Mac. Latest copy from VPS (8920 B, md5 `dfe0b377…`): DropdownMenu panel + `desktop.plugin.source` self-pull. Chat paste was the older 7989 B Popover file; installed the live VPS file instead. Path `~/.hermes/desktop-plugins/spend-tracker/plugin.js`. No named profiles. ⌘K Reload desktop plugins sent again (did not quit). |
| OT-59 | done | — | VPS → Mac desktop-plugin sync. Copied `vps-ui-sync` to `~/.hermes/desktop-plugins/vps-ui-sync/`. Installed `~/.hermes/bin/pull_vps_desktop_plugins.sh` (sources `hermes-vps.env`). `HERMES_VPS_SSH=hermes@hermes-droplet-campbell` in that env file and `~/.zshrc`. Filled launchd example at `~/.hermes/bin/ai.hermes.pull-vps-desktop-plugins.plist.example` (not loaded). Skill at `~/.hermes/skills/software-development/hermes-desktop-ui-over-vps/SKILL.md`. One rsync verified. ⌘K Reload desktop plugins sent. Did not quit Hermes. |
| OT-60 | done | — | At-risk Hermes source patches snapshotted outside `hermes-agent` so `hermes update` cannot wipe the only copy. Mac + VPS bundles in `~/.hermes/preserve/` (also on the VPS). Live tree left in place (runtime still needs it). Apply: `~/.hermes/preserve/apply.sh mac` / `vps`. Scripts/skills also copied to `preserve/scripts` and `~/.hermes/skills/software-development/hermes-desktop-chrome/`. Two VPS test files unreadable (root-owned). No secrets in patches. Desktop patches still need a rebuild after apply. |
| OT-61 | done | — | Preserve patches re-apply automatically after `hermes update`. Idempotent `apply.sh`. Mac zsh `hermes update` runs `after-update.sh --now`. VPS `~/bin/hermes` wraps `/usr/local/bin/hermes` the same way. `core.hooksPath` → `~/.hermes/preserve/githooks/post-merge` (20s delay) so dashboard/git pulls apply too. `HERMES_PRESERVE_AUTO=0` disables. Verified both trees: `tracked.patch already applied`. |
| OT-62 | done | — | Flipped to HostHatch. SSH `hermes-droplet-campbell` / `hermes-vps` = `hermes@100.82.91.60`. `droplet-campbell` = root on the same box. Old DO is `*-do` at `100.126.6.56`. Hatch gateway `/health` 200, serve 302, Telegram connected. DO gateway/serve stopped. Hermes login activates magenta `(hermes-venv)` prompt. WhatsApp and Gmail units not started (no Chrome / google-venv). DO overlay + secret inventory later closed as OT-66; owner may destroy that droplet. |
| OT-63 | done | — | HostHatch WhatsApp Baileys bridge (`:3000`, session reused, connected). Google Chrome 153 + `chromium-cdp` on `127.0.0.1:9222` (`/json/version` 200). Email adapter uses real Gmail SMTP `smtp.gmail.com:587` STARTTLS + IMAP `imap.gmail.com:993` (login probed OK). `gmail-reply-watcher` removed (IMAP is inbound). DO box stays up for inspection: `ssh droplet-campbell-do` (Tailscale) or `ssh droplet-campbell-do-pub` (`209.38.81.239`). DO `hermes-gateway.service` parked as `.off` so it cannot steal Telegram/WhatsApp. |
| OT-64 | done | — | Photon/iMessage already had credentials + numbers; sidecar on `:8789` (HTTP 401 = up). No new device login. Hourly model cost-guard timer enabled (1 min, $1/h → free OpenRouter). Preserve: new Hatch snapshot `vps-20260914T1725Z` (apply reverse-check OK); Mac `mac-20260913T2105Z` still applies. `~/bin/hermes` + `core.hooksPath` re-apply after `hermes update`. |
| OT-65 | done | — | Preserve is automatic with no owner step. Before `hermes update`: snapshot. After: apply (3-way fallback). Wired on Mac zsh `hermes()`, Mac `~/.local/bin/hermes`, Hatch `~/bin/hermes`, Hatch Desktop path `~/.local/bin/hermes`, and `hermes-agent` `post-merge` hook. |
| OT-66 | done | — | DO→Hatch overlay complete. Secrets that existed on DO are on Hatch (live auth kept; DO auth parked under `from-do`). Preserve apply OK (`vps-20260914T1819Z`). Gmail cron removed. Fork `main` `bc4ddbf10c`, private `.hermes` `main` `101f836`. Hatch `/health` 200. Owner may destroy the DigitalOcean droplet. |

## Standing rules for this list

- Never drop an `open` / `in_progress` / `held` row because a later message ignored it.
- Move a row to `done` only with evidence.
- Owner-requested stop-tracking moves a row to `later` (`docs/handover/future-tasks.md`) and is not repeated every turn.
- `later` is parked future work and is not repeated every turn.
