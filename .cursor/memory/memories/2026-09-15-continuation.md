# 2026-09-15 continuation

- Desktop update reached `main` `a55c972e09` (v0.21.3). Stock tree has no terminal wheel handler, so the Cursor overlay still did not scroll after update.
- Restored a wheel path on current `main`: `terminal-wheel.ts` encodes unused CSI `ESC[9001~` / `ESC[9002~` plus SGR 64/65 (not Home/`HwU`, not arrows, not C-u). `use-terminal-session.ts` hit-tests the fixed overlay and also uses `attachCustomWheelEventHandler`. FitAddon still freezes 500ms after a wheel. Vitest 4 passed.
- Hatch `patch-cursor-agent-wheel.py` now maps 9001/9002 (+ leftover Hw + SGR) to Ink `U()` with deps `[U,e]` (ignores inputDisabled `g`) and does not enable DECSET 1000. Applied to `2026.09.10-fd3934a` and `2026.09.08-6caf4ff` `1931.index.js`.
- Unsigned `Hermes.app` packed and ditto’d to `~/Applications/Hermes.app` (stamp `a55c972e09` dirty, 2026-09-15T08:50Z). Live app pid left running — reopen required to load the renderer.
- Five live `h-*` cursor-agent panes were `tmux respawn-pane` + `~/bin/agent --resume` so the new listener is in memory. Did not quit Hermes.app or restart the gateway.
- Owner reported 9001 leaking into the prompt and processes “stopping”. Cause: Ink treats `0-9` as a vim count; a parallel stdin listener does not consume those bytes. Desktop now sends Page Up/Down only. Hatch patch moved onto the existing `useInput` handler (`if(g)` still allows page/C-u). Park cron is not installed; `destroy-unattached` is off; six cursor-agent panes were still running at 08:57Z. Closing a Desktop tab can drop that SSH view; quit detaches the client. Reopen required to unload 9001.
- Owner asked to revert the terminal pane to default. Desktop `use-terminal-session.ts` restored to `a55c972e09`; wheel files deleted. Hatch `1931.index.js` restored from `.pre-hermes-wheel` on both Cursor versions; `patch-cursor-agent-wheel.py` is restore-only so `~/bin/agent` will not re-inject. Packed clean (stamp dirty=false). Installed; did not quit live Hermes. 9001 will continue until they reopen.
- Goal armed: Cursor transcript scroll. Stock wheel was xterm history. New path steals TUI wheel, sends Page Up/Down only, zeros xterm scrollback on TUI, Hatch `if(g)` still handles page keys. Tests 5 passed. App 09:28Z. Six panes `--resume`d. Goal stays open until reopen proves the transcript moves.
- OT-68: Hatch tmux clock forced to Sydney via `status-right` `TZ=Australia/Sydney date` (system stays UTC). `~/bin/hermes-term`, `hermes-term-resume`, `agent`, `~/.profile`, `~/.tmux.conf` updated. Desktop persist finished: restored tabs get `resumeOnCreate`, pane reveal on boot, quit keeps named tmux, tab close `kill-persist`. Tests 75+21. App ditto’d 09:38Z. Live Hermes pid left running — reopen required.
- OT-50: stock Ink only reached Page Up when `!inputDisabled && !promptActive`. A visible prompt swallowed `U()`. Patch now handles `pageUp`/`pageDown` first. Applied both 1931 versions; seven panes `--resume`d. Hatch proof: named `PageUp` on `h-cfff2c5c` changed the conversation body (tool-read tail → `submitRef` diff). Desktop wheel still needs reopen of the 09:41Z app.
- Desktop TUI latch: non-shell titles (chat names), Cursor chrome (`Run Everything`, `Cursor Grok`), or alt screen keep Page Up/Down on the Ink path; `zsh`/`user@host:` unlatch. Wheel tests 7. Signed pack failed (duplicate Apple Development identities). Unsigned pack ditto’d 09:48Z. Live Hermes pid 30783 not quit. Hatch clock still Sydney (19:47 AEST vs 09:47 UTC).
- Latch fix: wheel-time sync cannot unlatch a leftover `zsh` title. Restored tabs start latched. Live Hatch `h-cfff2c5c` title is `Hermes Desktop Fixes` `alt=0`. Both 1931 copies still have page keys before `g`/`v`. Unsigned pack ditto’d 09:51Z (Page Up/Down only; 9001 hits are Unicode width tables). Pid 30783 unchanged.
- Overlay hit-test: window wheel also accepts events whose target is inside `[data-terminal]` / `[data-persistent-terminal]`. Bundle assert script added. Tests 8. Pack `index-C0CcTvIJ.js` Page Up/Down only; ditto 09:54Z. Live Hermes still pid 30783 from 19:43.
- `planTuiWheelWrite` is the Desktop wheel decision+encode. Live Hatch case: normal buffer + `Hermes Desktop Fixes` → `\x1b[5~` only. Tests 10. Pack `index-xR7pw6mf.js`; ditto 09:56Z. Did not quit pid 30783.
- `createTuiWheelDispatcher` + window `WheelEvent` test writes Page Up. Hatch `PageUp` changed the live pane while the prompt (`~`) and Run Everything chrome were visible. Tests 11. Pack `index-sAXobJWW.js`; ditto 09:58Z. Pid 30783 still 19:43.
- Owner reported no scroll and no scrollbar after reopen. Native xterm scrollbar is intentionally off for Cursor TUI. Custom `data-tui-scrollbar` rail was missing from the installed pack. Shipped `index-Bw8wIGGE.js` with visible rail + host wheel listener + 12px pixel page; Force Reload applied. Attached Hatch pane `h-45ab6dfa` still scrolls on Page Up.
- Owner: new scrollbar still dead; tmux clock not Sydney. Clock root cause: tmux expands `%H`/`%M` inside `status-right` before `#()` runs, so `date` printed UTC. Fixed with `%%H:%%M %%Z` on Hatch `~/.tmux.conf` + `~/bin/hermes-term` (+ preserve); live status `20:30 AEST`. Scrollbar: rail was calling `scrollPages` when TUI latch missed (noop with scrollback 0). Now always writes Page Up/Down ×3. Pack `index-C9G8ZzFN.js`; Hermes quit+reopened (Force Reload left old renderer). Session `h-45ab6dfa` still attached.
- OT-68 owner-complete. OT-50 root cause: Ink `U()` no-ops when the live frame fits (todos+prompt) while tmux history holds the real transcript. Desktop Page Up/Down for persisted remote tabs now exec `tmux copy-mode` + `scroll-up`/`scroll-down` over the SSH ControlMaster (proven `pos=24 mode=1`). Typing cancels copy-mode. Hermes reopened with new main.
- OT-50 owner-complete: Desktop terminal scroll works. Goal closed.
- Persist+push: Mac snapshot `mac-20260915T1051Z`; `extras-after-apply.sh` re-patches cursor-agent + VPS hermes-term/tmux after every update. Fork branch `fix/desktop-terminal-scroll-and-persist` → NousResearch PR https://github.com/NousResearch/hermes-agent/pull/111822. Papership docs `d23cb1e` on origin/main. Hatch snapshot `vps-20260915T1053Z`.
- Preserve is automatic going forward: `post-commit` → `snapshot.sh` + `record-patch.sh agent-commit …`; `hermes update` still before/after; post-merge re-applies; snapshots also refresh `~/bin` + tmux + desktop-plugins backup. Mac preserve scripts brought up to Hatch-class. Pushed private `.hermes` `fabf5d9`. Owner does not need to ask each patch.
