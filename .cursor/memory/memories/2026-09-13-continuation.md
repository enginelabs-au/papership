# 2026-09-13 continuation

- Owner asked to fix cursor-agent conversation wheel (Ink TUI, not Hermes chat / xterm scrollback). 2026.09.10 `1931.index.js` viewport already had `U()` for keys and no wheel. Patched it to enable SGR mouse (`1000`/`1006`) and map wheel to `U()` (≤3 lines or page on a ≥5-event flick). `inputDisabled` still ignores. Did not inject PTY keys, steal `scrollTop`, or touch `vps-ui-sync`. Mapping test + `node --check` passed. Backup `1931.index.js.pre-hermes-wheel`. `~/bin/agent` re-applies via `preserve/scripts/patch-cursor-agent-wheel.py`. Existing live agent process unchanged until a new `agent`.
- Owner asked to keep fixing OT-50 scroll: bounce was gone because there was no scroll. Cause: we hid `.xterm-scrollable-element` overflow/scrollbar and capture-`preventDefault`’d the wheel, then forwarded SGR that Cursor did not use. Fifth pass restores native xterm scroll + the original visible viewport scrollbar. Kept POSIX `convertEol`, `lineHeight: 1`, fit hysteresis, passive wheel→fit freeze. Tests 19 passed. New preserve snapshot `~/.hermes/preserve/mac-20260913T2105Z` (apply.sh now picks newest `mac-2*`). Packed unsigned `Hermes.app` (asar 07:01) into `~/Applications` without quitting the live pane. Quit+reopen to load.
- Owner asked to automate preserve-patch apply after `hermes update`. No official Hermes post-update hook. Made `apply.sh` idempotent. Mac zsh `hermes()` runs `after-update.sh --now` after `update`. VPS `~/bin/hermes` wraps `/usr/local/bin/hermes` the same way. `core.hooksPath` points at `~/.hermes/preserve/githooks/post-merge` (20s delay) so in-app/git pulls also apply. `HERMES_PRESERVE_AUTO=0` skips. Dry-run: both trees already applied. OT-61 done.
- Owner asked to move at-risk Hermes updates into a wipe-safe home. Cannot relocate Electron/Python out of `hermes-agent` without breaking the live app/`Hermes.app`. Snapshotted Mac + VPS dirty trees into `~/.hermes/preserve/` (tracked.patch + untracked files + apply.sh). Copied scripts/skills out of the git tree. VPS copy at `/home/hermes/.hermes/preserve/`. Two root-owned VPS tests skipped. Secret scan clean (false `sk-` hits only). Plugins/config/`~/bin` already safe. After update: `apply.sh mac|vps`, then rebuild Desktop if those patches apply. OT-60 done.
- Owner asked to undo the last-request countdown + auto window-reload on `vps-ui-sync` (the pull script already manages sync). Mac file had been overwritten with that draft; VPS was unchanged. Restored via `pull_vps_desktop_plugins.sh` (5315 B, 15s poll, no reload). ⌘K Reload desktop plugins sent. Did not quit Hermes.
- Owner asked to copy VPS `desktop-plugins/vps-ui-sync/` to the Mac, install `pull_vps_desktop_plugins.sh`, and set `HERMES_VPS_SSH`. Copied plugin (5329 B). Script at `~/.hermes/bin/pull_vps_desktop_plugins.sh` sources `hermes-vps.env` (`hermes@hermes-droplet-campbell`). Same export added to `~/.zshrc`. Launchd example filled with that host/paths; not loaded (optional). Skill copied to `~/.hermes/skills/software-development/hermes-desktop-ui-over-vps/SKILL.md`. One rsync verified. ⌘K Reload desktop plugins sent. Did not quit Hermes. OT-59 done.
- Owner asked to install the updated spend-tracker plugin again. Chat paste was the older 7989 B Popover file; VPS `plugin.js` was newer (8920 B, 20:09Z) with DropdownMenu (no Tip/Popover) and a `desktop.plugin.source` self-pull. Copied VPS → Mac `~/.hermes/desktop-plugins/spend-tracker/plugin.js` (md5 `dfe0b377`). No named profiles. ⌘K Reload desktop plugins sent. Did not quit Hermes. OT-58 refreshed.
- Owner asked to install the VPS spend-tracker Desktop plugin on the Mac. Copied `/home/hermes/.hermes/desktop-plugins/spend-tracker/{plugin.js,README.md}` to `~/.hermes/desktop-plugins/spend-tracker/` (7989 B). No named profiles to also seed. Hermes was running; sent ⌘K → Reload desktop plugins via AppleScript. Did not quit the app (live Cursor pane). VPS `spend.snapshot` already exists. Look for `$x/$1` on the right status bar. OT-58 done.
- Owner asked to sleep/optimize the Chrome park for lower VPS load. Chrome was already gone; the login-enabled `chromium-cdp` unit would have brought ~256 MiB back. Disabled it from `default.target` (browser tools can still `systemctl start`). Idle now every 1 min: always kill leftover wwa/puppeteer; `systemctl stop chromium-cdp` unless Desktop is up and Chrome is busy (≥1% CPU or <120s old). Did not SIGSTOP — that holds RSS on a 4 GiB box. Gateway + Gmail watcher stay up. Verified: cdp down, no chrome procs, load 0.12, ~1.2 GiB available. OT-57 done.
- Owner rejected waking messaging with `hermes-msg` and rejected Hermes chat looking for “sleep”/“wake”. Slack/Telegram live in `hermes-gateway`, so stopping that unit was the only reason a wake command existed. Revised idle: gateway + Gmail watcher stay up (phone inbound works with no command). Deterministic keep-alive is Desktop isolated serve (`serve --isolated`). After 20 min with no Desktop and no inbound, only leftover Chrome/wwa is killed. Bare `hermes-msg` is status. Verified on droplet: `desktop=yes`, idle tick left core up. Email still off. OT-56 revised.
- Owner said VPS Cursor scrolling stopped entirely after the 05:29 wheel lock. Capture `preventDefault` stopped the viewport bounce and also stopped xterm from encoding wheel to the PTY. Fourth pass still owns the wheel on alt/mouse surfaces, then writes SGR wheel (`64`/`65`) or alt-buffer arrows into the PTY. Tests 26 passed. Unsigned app ditto’d (asar 05:48). Quit+reopen. OT-50 still in_progress.
- Owner asked messaging (WhatsApp/Telegram/Slack/iMessage/Gmail) to sleep after inactivity without re-enabling platforms or `hermes gateway restart`. Slack/Telegram live inside the gateway process, so the honest park is to **stop** `hermes-gateway` + Gmail watcher after 20 min with no inbound/`sessions.json`/wake stamp, and start them with `hermes-msg`. Timer every 5 min. Sleep/wake verified. Email platform still false (OT-49). `hermes-serve` + SMTP relay stay up. OT-56 done.
- Owner asked to stop rogue droplet drain so one Cursor pane can finish, and put other Hermes work in a passive wake-on-interact state. SSH was wedged (load ~50) then came back on public IPv4. Kept live `cursor-agent` 1179536; killed two extras (one `--resume`, one SIGSTOP’d persist pane still holding 616 MiB RSS); killed a leftover `hermes sessions export` of the old Luna loop chat; stopped root WhatsApp `wwa` daemon + Chrome. Load 50→6, available RAM ~1.8 GiB. Gateway + serve + Gmail watcher left running (Desktop needs them; cheap vs Cursor). True RAM-sleep is not possible for Cursor — SIGSTOP still occupies RSS — so extras **exit** while one pane is interactive (`~/bin/hermes-term-sleep-wake` cron). Wake: attach the tmux tab and `agent --resume` / new `agent`. WhatsApp stays down until that platform is used again. Upgrade still required. OT-55 done.
- Owner reported the 05:19 OT-50 binary turned Cursor wheel into a bounce loop (scrolls up a bit, snaps back). Cause: xterm 6 SmoothScrollableElement still owns wheel when mouse-wheel protocol is off; TUI redraw `_sync`s ydisp. Locked the viewport on alt-screen / mouse-tracking (`scrollback` 0, `scrollSensitivity` 0, capture-phase `preventDefault` so the scroller returns early), froze FitAddon 500ms after wheel. Tests 22 passed. Unsigned app ditto’d to `~/Applications/Hermes.app`. Did not quit the running app. OT-50 still in_progress until reopen.
- Owner asked why the Hermes gateway keeps dropping and whether 2 Cursor panes + Hermes overloads the droplet. Yes. Box is 2 vCPU / 3.8 GiB / no swap. Live load 54–58, RAM 3.7/3.8 GiB used (83–145 MiB available). Three `cursor-agent` (~1.5 GiB), isolated+public serve, gateway, WhatsApp Puppeteer Chrome, Hermes Chromium, Postgres. Kanban logged `memory pressure is critical`. Slack/Telegram/WhatsApp sockets fail; Desktop then drops liveness and SSH (21 drops / 88 timeouts in `desktop.log`; latest 19:20Z). Email still off. Killed a stuck `cursor-agent ls`. Upgrade in DO: 4 vCPU / 8 GiB floor; 8/16 if two Cursor panes stay. OT-54 done.
- Owner said Cursor was still glitching after the 05:13 reopen (first OT-50 binary was live). Remaining loop: `lineHeight: 1.12` → 12.32px cells plus 1px overlay floor/ceil, so FitAddon flipped 1 row and the 160ms gate still WINCH'd ~6Hz. Set `lineHeight: 1`, never apply 1-cell fits, snap overlay rect by 1px. Tests 20 passed. Unsigned app reinstalled. Droplet SSH timed out after a hung `tmux set-option`; no VPS change required for this pass. OT-50 still in_progress until the next quit+reopen.
- Owner asked Hermes Desktop terminals to survive quit and restore Cursor on reopen (type `/resume` in each pane). Desktop quit still HUPs `ssh -tt` PTYs, so `/resume` at a fresh bash prompt would do nothing. Implemented named tmux per tab (`h-<tabId>`): quit detaches; reopen remounts persisted tabs and `tmux attach`. If the session is gone, `hermes-term-resume` runs `agent --resume <id>` (or claims the next recent chat, else `--continue`) — the CLI equivalent of TUI `/resume`. VPS helpers in `~/bin`. `agent resume` now also gets `--yolo`. Tests: persist/ssh/terminals/persistent 96 passed. Unsigned app installed; did not quit the running app. OT-53 in_progress until reopen. First quit still kills today's non-tmux Cursor panes.
- Owner asked Hermes to chime when Cursor finishes on the VPS. The droplet already had a stop/sessionEnd script that wrote BEL+OSC 9 to every pts (inaudible on the Mac, and noisy for TUIs). Desktop now handles BEL/OSC 9: `afplay` Glass + a “Cursor finished” toast (debounced 1.5s). Script now rings only `$SSH_TTY`. Tests 15 passed. Unsigned app copied to `~/Applications`. Same quit+reopen as OT-50. OT-52 in_progress until heard.
- Owner asked for droplet Cursor sessions to always Run Everything. `~/.cursor/cli-config.json` was already `approvalMode: unrestricted` / sandbox disabled; the TUI still started without `--yolo`. Added `/home/hermes/bin/agent` wrapper (`--yolo --sandbox disabled --approve-mcps`; login/update/status/worker unwrapped) and prepended `~/bin` at the end of `.profile` so login shells (Desktop `ssh -tt`) hit it before `~/.local/bin/agent`. New `agent` only. OT-51 done.
- Owner reported Hermes Desktop terminal rapid back-and-forth scroll in Cursor CLI sessions, worse with slash menu or manual scroll. Not a Cursor-agent or Hermes-chat fix: Desktop xterm host. `convertEol: true` on POSIX plus FitAddon WINCH when the viewport scrollbar takes width. Added `shouldApplyTerminalFit` hysteresis, POSIX `convertEol` off, hidden `.xterm-viewport` scrollbar. Tests 29 passed. Unsigned pack installed to `~/Applications/Hermes.app` (dirty stamp on `9fd6983c20`). Did not quit the running app (would HUP the live Cursor pane). OT-50 in_progress until reopen+verify.
- Owner asked why the gateway keeps restarting after re-enabling Email. Logs: IMAP pass, SMTP timed out ~90s each cycle from 15:35Z; reconnect; watchdog exit 75 at 15:58Z (loop blocked). Desktop also requested several gateway restarts. `platforms.email.enabled` is false again; unit stable from 16:03Z. Keep Email disabled — OT-48 did not unblock DigitalOcean SMTP; env SMTP host still wins over yaml 127.0.0.1. OT-49 done.
- Owner asked to un-block SMTP for Hermes on the droplet. Probes: smtp.gmail.com 25/465/587 timeout; imap.gmail.com 993 open; gmail.googleapis.com 443 open. UFW already allows outbound 465/587 — this is DigitalOcean's SMTP filter, not a local rule. Did not re-enable `platforms.email` (that adapter probes Gmail SMTP and times out). Installed `gmail_api_send.py` + `gmail_smtp_relay.py` as user hermes, systemd `gmail-smtp-relay` on 127.0.0.1:2525 (EHLO 250). Tightened `google_token.json` to 0600. Patched droplet email `_standalone_send` to use Gmail API. Isolated serve restarted. No test email sent. OT-48 done.
- Owner asked why Luna wastes tokens on repeated inspect dumps. Screenshots show the same `[Your active task list was preserved across context compression]` A1/A2 todos plus the same `hermes_tools` terminal dump (~39k stdout) after every compress. That is a compaction→todo-replay→re-dump loop; live process/config output changes a few bytes so identical-call stubbing does not fire. Set `tool_loop_guardrails.hard_stop_enabled: true` on Mac + droplet. Stop that chat; start a narrow new one. OT-47 done.
- Owner screenshots: max tool-calling iterations then wrap-up 400 `reasoning.effort` invalid. Droplet logs: sessions `20260913_100141_76aa46` and `20260913_144852_3a8556` on `openai/gpt-5.6-luna` ended `max_iterations_reached(100/100)` (one session twice; tool_turns 101 then 159). Both sessions store `reasoning_config.effort: ultra`. Main loop already clamps `ultra`→`max`; `_iteration_summary_chat_kwargs` forwarded raw `ultra` and OpenRouter 400ed (#89503). Added `_summary_reasoning_extra_body` in local + droplet `chat_completion_helpers.py`. Isolated serve killed so Desktop reconnects onto the patch. `test_reasoning_effort_wire_translation.py` 5 passed. Goal footer `0/20` is a different counter (one user/continue = one goal turn; 100 tool steps are inside it). OT-46 done.
- Owner asked to set all Hermes turn/tool limits to 100, whether infinite is possible/should be used (do not apply), and why 30→100 still hit. Set Mac `~/.hermes/config.yaml` and droplet `/home/hermes/.hermes/config.yaml` (edit as hermes): `agent.max_turns`, `code_execution.max_tool_calls`, `goals.max_turns`, `delegation.max_iterations` all 100. Droplet delegation was 250. Infinite not set (`0`/`none`/`unlimited`/`infinite` resolve to `sys.maxsize`). `tool_loop_guardrails.hard_stop_enabled` remains false. Existing live agent keeps the old baked-in `max_iterations` until a new Desktop chat. OT-45 done.
- Owner reported Hermes Desktop “could not safely reserve this session” at 13:56Z. App was running; remote isolated serve as hermes was up; backend ready. Claim fails closed when `try_acquire_active_session` cannot prove ownership. Droplet `/home/hermes/.hermes/runtime/active_sessions.json` was `root:hermes` 0600 (empty entries) from the earlier root SSH isolated serve. Hermes user could not read it. Chowned json + wake-word.lock to hermes; verified readable/writable. Retry the chat turn. Stay on `hermes@`. OT-44 done.
- Owner asked 2026-09-13 why cmux did not inherit the Terminal home cwd, and to adopt the same change. cmux is a separate Ghostty app: it restores `session-com.cmuxterm.app.json` and, by default, new workspaces inherit the current cmux workspace (`~/papership`). It does not read Cursor `terminal.integrated.cwd` or Terminal.app working-directory behavior. Set `app.workspaceInheritWorkingDirectory: false` in `~/.config/cmux/cmux.json`, created `~/.config/ghostty/config` with `working-directory = home`, quit cmux, rewrote the saved session to `/Users/camdouglas`, relaunched. Session stayed on home. `cmux reload-config` from outside the app is denied. OT-43 done.
- Owner asked 2026-09-13 to make new Mac terminals start at `/Users/camdouglas` instead of papership. No `cd` in zsh; Cursor was using the open workspace, and Terminal.app new tabs inherited the current folder. Set Cursor user `terminal.integrated.cwd` to `/Users/camdouglas` and `splitCwd` to `initial`. Set Terminal.app `NewWindowWorkingDirectoryBehavior` and `NewTabWorkingDirectoryBehavior` to 0 (home). Did not add `cd ~` to zshrc. OT-42 done.
- Owner asked 2026-09-13 to do both leftover actions from OT-40: kill the root isolated serve and point Desktop at `hermes@`. Quit Hermes, killed `/root/.hermes/desktop-ssh` isolated processes (none left after quit). Set `user: hermes` in Application Support `connections.json` and `connection.json`. Split `~/.ssh/config`: `droplet-campbell` stays root; `hermes-droplet-campbell` + `hermes-vps` are hermes / no TTY. Reopened Hermes. Log: `opening control master to hermes@hermes-droplet-campbell:22`, remote backend ready. Isolated serve PID as user hermes, token under `/home/hermes/.hermes/desktop-ssh/`. No `/root/` isolated serve. `auth.json` still hermes. OT-41 done. Values not printed.
- Owner asked 2026-09-13 to diagnose a lost droplet connection. The box was up: SSH `hermes-vps` / `droplet-campbell` worked; Tailscale `hermes-droplet-campbell` (`100.126.6.56`) active; `hermes-serve` listening `0.0.0.0:9119` (health 302); `hermes-gateway` on `127.0.0.1:8642` (GET `/health` 200). Desktop logs showed repeated `SSH operation to root@hermes-droplet-campbell timed out` from 13:38Z. Host load was ~66/66/47 on 2 cores (WhatsApp bridge + photon sidecar + Chrome + a 6h root isolated `hermes serve`). Gateway watchdog missed 3 liveness probes and exited 75 at 13:43Z; systemd restarted it. `/home/hermes/.hermes/auth.json` (and checkpoints / `nous_auth.json`) were `root:root` because Desktop SSH is `root@` and the isolated serve rewrote them at 13:43Z — hermes-user gateway could not read tokens. Restored ownership to `hermes:hermes` (readable). Local Papership tunnel `127.0.0.1:8642`/`9119` was down (separate from Desktop). Residual: reconnect Desktop as `hermes`, not `root`; leftover root isolated serve PID from 07:31Z. OT-40 done. Values not printed.
